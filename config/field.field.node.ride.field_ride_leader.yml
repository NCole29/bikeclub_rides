langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_ride_leader
    - node.type.ride
id: node.ride.field_ride_leader
field_name: field_ride_leader
entity_type: node
bundle: ride
label: Leader(s)
description: 'Start typing a name in the field to see a list of ride leaders (must be a club member and designated leader); then select from the list.  <em>Note: If a leader has disabled their personal contact form, their name will appear without a link.</em>'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:user'
  handler_settings:
    target_bundles: null
    sort:
      field: name
      direction: ASC
    auto_create: false
    filter:
      type: role
      role:
        ride_leader: ride_leader
        board: '0'
        events_coordinator: '0'
        administrator: '0'
        member: '0'
        membership: '0'
        site_admin: '0'
        rides_coordinator: '0'
    include_anonymous: false
field_type: entity_reference
