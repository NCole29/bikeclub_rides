langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_cancel
    - node.type.recurring_date
id: node.recurring_date.field_cancel
field_name: field_cancel
entity_type: node
bundle: recurring_date
label: Cancel
description: "Canceled recurring rides remain on the calendar and are marked canceled.\r\nIn contrast, removed \"instances\" of a recurring ride are not on the calendar.\r\n"
required: false
translatable: true
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
