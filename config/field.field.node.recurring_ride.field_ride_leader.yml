langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_ride_leader
    - node.type.recurring_ride
id: node.recurring_ride.field_ride_leader
field_name: field_ride_leader
entity_type: node
bundle: recurring_ride
label: Leader(s)
description: "Begin typing name in field to get a list of ride leaders and select from list. Leader must be a club member and designated ride leader. The ride leader's name appears without a link to their contact form if they disabled the personal contact form in their account settings."
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:user'
  handler_settings:
    target_bundles: null
    sort:
      field: name
      direction: ASC
    auto_create: false
    filter:
      type: role
      role:
        ride_leader: ride_leader
        administrator: '0'
        member: '0'
        board: '0'
        events_coordinator: '0'
        membership_coordinator: '0'
        rides_coordinator: '0'
        content_creator: '0'
        editor: '0'
        site_admin: '0'
    include_anonymous: true
field_type: entity_reference
